name: api_server
description: today-lunch API server
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4

dev_dependencies:
  test: ^1.25.0
